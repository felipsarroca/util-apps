# Github Sync (Electron)

## Requisits
- Node.js LTS
- Git instal·lat i al PATH
- GitHub CLI (`gh`) instal·lat i **autenticat** (`gh auth status`)

## Instal·lació i arrencada
```bash
npm install
npm run start
